{
  "name": "my-ai-web",
  "version": "1.0.0",
  "description": "- Create project directory `my-ai-web` - Initialize `package.json` with Express and an AI SDK (like `openai` or `langchain`) - Create `server.js` for the Express backend - Create `index.html` for the frontend - Write a `Dockerfile` for deployment - Start the container via Docker",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.18.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "lowdb": "^3.0.0",
    "openai": "^6.48.0",
    "pdfkit": "^0.19.1"
  }
}
